{
  "name": "@edunexia/database-schema",
  "version": "0.1.0",
  "description": "Tipos TypeScript para o esquema do banco de dados da Edunéxia",
  "main": "index.ts",
  "types": "index.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint . --ext ts --report-unused-disable-directives --max-warnings 0",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@edunexia/auth": "workspace:^",
    "@supabase/supabase-js": "^2.39.3"
  },
  "devDependencies": {
    "typescript": "^5.0.2"
  }
}