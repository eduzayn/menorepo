# Variáveis de ambiente para o Docker Compose
# Copie este arquivo para .env antes de executar docker-compose

# Configurações gerais
NODE_ENV=production

# Portas externas (utilize se precisar alterar as portas padrão)
# NGINX_HTTP_PORT=80
# NGINX_HTTPS_PORT=443

# Configurações do Supabase (comum a todos os módulos)
# IMPORTANTE: Substitua estas variáveis com os valores reais do seu ambiente Supabase
# Você pode copiar estes valores dos arquivos .env que já utiliza em desenvolvimento
VITE_SUPABASE_URL=seu_url_do_supabase
VITE_SUPABASE_ANON_KEY=sua_chave_anonima_do_supabase
VITE_SUPABASE_SERVICE_ROLE_KEY=sua_chave_service_role_do_supabase
VITE_SUPABASE_JWT_SECRET=seu_jwt_secret_do_supabase

# APIs compartilhadas
VITE_API_URL=http://api:8000
VITE_WS_URL=ws://api:8000

# URLs dos módulos (utilize para customizar URLs em produção)
VITE_APP_URL=http://localhost
VITE_SITE_EDUNEXIA_URL=http://localhost
VITE_COMUNICACAO_URL=http://localhost/comunicacao
VITE_CONTABILIDADE_URL=http://localhost/contabilidade
VITE_FINANCEIRO_URL=http://localhost/financeiro-empresarial
VITE_MATERIAL_DIDATICO_URL=http://localhost/material-didatico
VITE_MATRICULAS_URL=http://localhost/matriculas
VITE_PORTAL_ALUNO_URL=http://localhost/portal-do-aluno
VITE_PORTAL_PARCEIRO_URL=http://localhost/portal-parceiro
VITE_PORTAL_POLO_URL=http://localhost/portal-polo
VITE_RH_URL=http://localhost/rh

# URLs de redirecionamento para o fluxo de pagamento (usado em matriculas)
PAYMENT_SUCCESS_URL=http://localhost/matricula/sucesso
PAYMENT_CANCEL_URL=http://localhost/matricula/cancelado

# Gateway Lytex (usado em matriculas)
VITE_LYTEX_ATIVO=false
VITE_LYTEX_BASE_URL=https://api.lytex.com.br
VITE_LYTEX_CLIENT_ID=seu-client-id
VITE_LYTEX_CLIENT_SECRET=seu-client-secret
VITE_LYTEX_CALLBACK_URL=http://localhost/api/pagamentos/lytex/webhook

# Gateway InfinitePay (usado em matriculas)
VITE_INFINITEPAY_ATIVO=false
VITE_INFINITEPAY_BASE_URL=https://api.infinitepay.io
VITE_INFINITEPAY_CLIENT_ID=seu-client-id
VITE_INFINITEPAY_CLIENT_SECRET=seu-client-secret
VITE_INFINITEPAY_CALLBACK_URL=http://localhost/api/pagamentos/infinitepay/webhook

# Configurações específicas de módulos
# Matriculas
VITE_DIAS_BLOQUEIO_ACESSO=10
VITE_DIAS_CANCELA_CONTRATO=30
VITE_DIAS_BLOQUEIO_INADIMPLENCIA=30
VITE_DIAS_CANCELA_INADIMPLENCIA=90

# Configurações de integração entre módulos
VITE_ENABLE_MODULE_INTEGRATION=true

# Configurações de build
BUILD_TARGET=production  # ou development
