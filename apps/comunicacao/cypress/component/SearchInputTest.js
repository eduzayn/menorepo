/**
 * Script para executar testes no componente SearchInput
 * 
 * Este arquivo serve como um exemplo de como conduzir testes de componente
 * usando Cypress. Os testes devem ser executados via CLI com o comando:
 * `pnpm test:component`
 * 
 * Importante: Certifique-se que o componente SearchInput possui:
 * - Um atributo data-cy="search-input" no campo de entrada
 * - Um atributo data-cy="search-icon" no ícone de busca
 * - Um atributo data-cy="clear-button" no botão de limpar
 * - Um atributo data-cy="search-container" no container principal
 */

// Este arquivo é apenas um utilitário e não contém lógica de teste real.
// Os testes estão em SearchInput.cy.tsx
console.log('Para executar os testes do SearchInput, use: pnpm test:component'); 